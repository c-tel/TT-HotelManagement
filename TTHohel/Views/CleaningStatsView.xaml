<UserControl x:Class="TTHohel.Views.CleaningStatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TTHohel.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel LastChildFill="True" Background="#17252B" >
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="30" DockPanel.Dock="Top">
            <DatePicker SelectedDate="{Binding SelectedDate, Mode=TwoWay}" Margin="0 0 10 0"></DatePicker>
        </StackPanel>
        <DataGrid ItemsSource="{Binding Stats}" 
                  IsReadOnly="True" 
                  DockPanel.Dock="Top" 
                  AutoGenerateColumns="False"
                  HorizontalAlignment="Center"
                  Width="285">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Прізвище" Binding="{Binding Surname, Mode=OneWay}"/>
                <DataGridTextColumn Header="К-сть виконаних прибирань" Binding="{Binding CountCompleted, Mode=OneWay}"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
